# 2025

# test
ls -l find_letter.c; cat find_letter.c
-rwxr-xr-x 1 me なし 818 Nov 19 16:13 find_letter.c
﻿/**

氏名: 渡邊鉄太

学生番号: B255451

作成日: 2025-11-17

入出力の説明:

動きの説明:

感想:

*/
#include <stdio.h>

/* ここにfind_letter関数を定義する */
int find_letter(char str[], char x) {
  int i = 0;
  while (str[i] != '\0') {
    if (str[i] == x)
      return i;
    i++;
  }
  return -1;
}

int main() {
  char x;
  char str[100 + 1]; // 100文字以下の文字列
  int location, i;

  // デバッグ用
  for (i = 0; i < 100 + 1; i++)
    str[i] = 'a'; // scanfで読み込んだ文字列より後の配列要素をあらかじめ'a'で埋めておく．

  x = getchar();
  scanf("%100s", str);

  /* ここでfind_letter関数を呼び出し，戻り値を変数locationに代入する */
  location = find_letter(str, x);
  printf("%d\n", location);
  return 0;
}
テスト 11 成功（pass）
テスト 12 成功（pass）
すべてのテストに成功しました．(12)

# test
diff -ubs 直前のfind_letter.c find_letter.c
--- tests/find_letter_prev.c	2025-11-19 16:14:04.560950500 +0900
+++ find_letter.c	2025-11-19 16:18:06.367681900 +0900
@@ -7,10 +7,14 @@
 作成日: 2025-11-17
 
 入出力の説明:
+  標準入力から探す文字を変数xに読み込む．
+  続いて，空白を含まない文字列を配列strに読み込む．長さが100文字を超える場合は100文字まで読み込む．
+  文字列str中の文字xの位置を表示する．文字列の最初の文字がxであれば0を，2番目の文字がxであれば1を，i番目の文字がxであればi-1を出力する．
+  配列 str[]中に文字xが現れない場合は-1を返す．
 
-動きの説明:
+動きの説明:文字列の最後には\0があるのでそれが出てくるまでxの文字を探す．見つかったらその位置を返し，見つからなければ-1を返す．
 
-感想:
+感想:return文の使い方が上手になった気がする.
 
 */
 #include <stdio.h>

テスト 11 成功（pass）
テスト 12 成功（pass）
すべてのテストに成功しました．(12)
